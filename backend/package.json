{
  "name": "photos-album",
  "version": "1.0.0",
  "description": "",
  "main": "./app/app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "nodemon --watch '**/*.ts' --exec 'ts-node' server.ts",
    "docker": "nodemon --watch '**/*.ts' --exec 'ts-node' server.ts",
    "prod": "node ./build/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.6",
    "express": "^4.17.1",
    "mongoose": "^5.9.11"
  },
  "devDependencies": {
    "nodemon": "^2.0.3",
    "ts-node": "^8.8.2",
    "typescript": "^3.7.5"
  }
}
